<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tumbler.manager.admin.dao.AdminDAO">
<select id="managerLogin" parameterType="manager_admin" resultType="manager_admin">
select * from admin where a_id = #{a_id} and a_password = #{a_password}
</select>
</mapper>